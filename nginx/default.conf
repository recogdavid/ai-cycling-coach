# ============================================================
# HTTP → HTTPS redirect and Certbot challenge handler
# ============================================================
server {
    listen 80;
    listen [::]:80;
    server_name dabosch.fit www.dabosch.fit n8n.dabosch.fit ai.dabosch.fit;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# ============================================================
# HTTPS reverse proxy to n8n + static site
# ============================================================
server {
    http2 on;
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name dabosch.fit www.dabosch.fit;

    ssl_certificate /etc/letsencrypt/live/dabosch.fit/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/dabosch.fit/privkey.pem;

    ssl_session_timeout 1d;
    ssl_session_cache shared:MozSSL:10m;
    ssl_session_tickets off;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header Referrer-Policy "no-referrer-when-downgrade";

    root /usr/share/nginx/html;
    index index.html;

    # Proxy both webhook and webhook-test
    location ~ ^/webhook(-test)?(/.*)?$ {
        proxy_pass http://n8n:5678;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    # Proxy LibreChat OAuth callbacks from main domain
    location /api/actions/ {
        proxy_pass http://librechat-dabosch:3080/api/actions/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_connect_timeout 1200s;
        proxy_send_timeout    1200s;
        proxy_read_timeout    1200s;
    }
    # Proxy API routes
    location /api/ {
        proxy_pass http://n8n:5678/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # Proxy OAuth callbacks from ai.dabosch.fit to main domain
    location /ai-oauth/ {
        proxy_pass http://librechat-dabosch:3080/oauth/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_connect_timeout 1200s;
        proxy_send_timeout    1200s;
        proxy_read_timeout    1200s;
    }

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

# ============================================================
# HTTPS reverse proxy for n8n editor
# ============================================================
server {
    http2 on;
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name n8n.dabosch.fit;

    ssl_certificate /etc/letsencrypt/live/dabosch.fit/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/dabosch.fit/privkey.pem;

    ssl_session_timeout 1d;
    ssl_session_cache shared:MozSSL:10m;
    ssl_session_tickets off;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    location / {
        proxy_pass http://n8n:5678/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# ============================================================
# HTTPS reverse proxy for LibreChat (NEW)
# ============================================================
server {
    http2 on;
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name ai.dabosch.fit;

    ssl_certificate /etc/letsencrypt/live/dabosch.fit/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/dabosch.fit/privkey.pem;

    ssl_session_timeout 1d;
    ssl_session_cache shared:MozSSL:10m;
    ssl_session_tickets off;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;
    # Fix undefined in OAuth redirects
    if ($args ~ "(.*)redirect_uri=undefined(.*)") {
        set $args $1redirect_uri=https://ai.dabosch.fit$2;
    }
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header Referrer-Policy "no-referrer-when-downgrade";
    location /api/ {
        proxy_pass http://librechat-dabosch:3080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_connect_timeout 1200s;
        proxy_send_timeout    1200s;
        proxy_read_timeout    1200s;
    }
    location / {
        proxy_pass http://librechat-dabosch:3080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        proxy_connect_timeout 1200s;
        proxy_send_timeout    1200s;
        proxy_read_timeout    1200s;
    }

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}
